{
  "name": "cra-expressjs-docker-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/express": "^4.17.12",
    "@types/node": "^12.0.0",
    "@types/nodemon": "^1.19.0",
    "express": "^4.17.1",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "server": "nodemon --config nodemon.json --exec ./node_modules/.bin/ts-node -- app/index.ts ",
    "server:tsc": "tsc --project server/tsconfig.server.json && Set NODE_ENV=Development&&nodemon server"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0"
  }
}
